'use strict';

const proxyqire = require('proxyquire');
const expect = require('chai').expect;
const sinon = require('sinon');

require('../../ws.repository');

describe('WS-CLI service', () => {
  it('TODO: to be implemented', sinon.test(function (done) {
    done();
  }));
});
